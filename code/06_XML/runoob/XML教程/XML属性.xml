<?xml version="1.0" encoding="utf-8" ?>
<note>
  <!--
    XML属性应当尽量避免使用:
      1.属性不能包含多个值(元素可以)
      2.属性不能包含树结构(元素可以)
      3.属性不容易扩展(破坏XML易于扩展的优点)且难以阅读和维护
    有关数据的数据-元数据(如标识符)应当存储为属性,而数据本身应当存储为元素
  -->
</note>
